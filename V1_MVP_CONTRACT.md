# Product Visual Studio — V1 MVP 执行契约 v0.8

状态：运行规则修订 v0.8（同步用户交互规则与 PRD），待用户实测  
日期：2026-08-30

## 1. 真实目标

围绕一个明确 SKU 建立可信的产品母版，并从该母版完成三种纯图片结果：白底交付、指定背景融图、参考视觉重建。Skill 负责判断路径、控制产品保真、生成与 QA；用户不需要理解内部模式编号。

## 2. V1 输入与素材身份

- 产品视觉事实基准：原始产品图；用于核验图片中可见的颜色、结构、配件、文字、Logo 与外观特征。
- 用户确认的产品身份信息：SKU、版本、尺寸或其他无法仅凭图片确认的信息，必须由用户明确提供或确认。
- 产品母版：后续生产使用的已确认图片，可由原始产品图精修得到，也可由用户直接上传已精修图。
- 指定背景图：路径 B 的目标场景来源。Candidate A 使用其现有镜头、空间结构和主要陈列关系；Candidate B 保留可识别的场景身份与视觉语言，但允许生成式重建镜头、构图和周边物体关系。
- 视觉参考图：路径 C 只提供构图、场景、光影、色彩、材质、层次与信息区方法，不提供用户产品事实或商业文案。
- 目标交付像素：必须给出具体像素；“8K”“高清”或比例不能替代像素值。

若用户只提供已精修产品图而没有原始产品图，允许在用户确认其 SKU、颜色、版本与外观正确后继续，并标记为“用户确认的产品母版（未与原始素材交叉验证）”。AI 生成结果一开始不是产品事实来源；但如果用户明确指出某张生成图并确认“可以作为产品母版”，允许按用户指定的工作角色提升。必须保留生成来源、原始证据边界和已说明风险，不得把这些细节改写成原始素材已经独立验证过的事实。“无修改意见”本身不等于母版提升。

产品母版的工作角色确认与细节确认分开记录：用户说“以后按这张图做”，足以让该图承担 Product Master 的工作角色，但不等于用户逐项确认了图中的每个可见细节。只有用户明确点名的可见外观或结构，才能记录为用户确认的细节；其余内容仍标记来源和验证范围。若该图来自 Path B / C 场景，只把识别出的产品区域和用户确认的产品表现纳入产品依据，不把背景、道具、人物、环境光、遮挡或透视变形纳入产品事实。

## 3. 共同入口：确认产品母版

1. 接收原始产品图或已精修产品图。
2. 检查完整轮廓、可见结构、颜色、配件、文字 / Logo、裁切、遮挡与可用像素；要求用户提供或确认无法由图片核验的 SKU、版本等信息。
3. 原图需处理时，执行白底保真精修；已精修图合格时不得强制重修。
4. 只有用户确认产品母版后，才能进入路径 B 或 C。
5. 只有正面图时，不得把新生成的背面、侧面或 3/4 视角描述为原始证据已验证；应补充对应角度，或经用户同意把结果作为带说明的产品母版工作依据 / 创意方向图使用。若用户明确指定生成图为产品母版，允许按该指定角色使用，但必须保留“用户确认、未与原始素材交叉验证”的来源说明。

## 4. 三条 V1 路径

### 路径 A：白底产品图

输入：产品图 + 目标交付像素。  
处理：清理背景、白边、灰尘和瑕疵，改善清晰度、材质与基础光影，同时保持结构、比例、颜色、配件、文字与 Logo。  
输出资产类型：产品母版或白底交付图。  
停止点：用户下载，或明确选择路径 B / C。

### 路径 B：指定背景融图

输入：已确认产品母版 + 一张指定背景图 + 目标交付像素。  
默认候选结构固定为两种制作逻辑，但具体镜头、落位、食物与陈列方案必须根据当前产品母版和背景动态制定：  

- Candidate A｜原场景锁定融图：直接把产品放进用户上传的背景。保留原背景的镜头、空间结构、主要物体和整体陈列关系；只允许为目标比例进行已说明的裁切或补边，并增加产品、接触阴影及必要的局部光色融合。未经用户许可，不移除或重排仍然可见的场景物体。对移除桌布等局部背景调整，生成前只能作为建议，除非用户明确提出或确认，不得写成已经授权。“锁定”默认表示场景身份、镜头与主要关系锁定，不表示背景像素级完全不变。
- Candidate B｜场景语言创意重拍：保留参考场景的材质、色彩、光线、气质与可识别身份，为产品重新设计镜头、构图和陈列。可以移除妨碍商品展示的物体、调整周边关系并加入相关食物或道具，但产品必须始终是第一视觉主体，且不得虚构产品母版不支持的结构。

候选策略：默认同时规划 Candidate A / B，不再要求用户先选择制作方法。两张图共享同一个已确认产品母版、SKU 身份、目标像素、核心业务目标和产品保真边界；差异来自“直接融入原场景”与“沿用场景语言重新拍摄”两种制作逻辑，而不是产品大小、轻微裁切或道具移动。每张候选必须独立通过产品证据、物理落位、商品第一主体与授权边界等硬过滤。只有原背景不存在物理有效落位、素材证据不足或另一条路线无法满足保真边界时，才降级为单候选并说明原因。  
输出资产类型：场景融合图。  
边界：Candidate B 仍属于路径 B，因为它必须保留这一个指定场景的可识别身份；路径 C 学习的是可迁移的视觉方法，可以重新实现具体场景。若用户明确要求产品或背景像素级不变，而当前方法只能生成式重绘，必须停止并说明、改用可验证的确定性处理，或由用户明确同意放宽像素约束。

### 路径 C：参考视觉重建

输入：已确认产品母版 + 一张视觉参考图 + 目标交付像素。  
处理：提取视觉蓝图，分离产品事实、可借鉴视觉策略、必须重建的来源身份锚点与排除项；先向用户展示两条有意义的 Candidate A / B 方案并获得确认，再生成原创视觉底图。生成后征求修改意见；没有修改意见时，不要求用户再从 A / B 中二选一。  
输出资产类型：原创视觉底图；存在未验证角度或结构时只能作为创意方向图。  
边界：不得保留竞品产品身份、品牌、Logo、文案、认证、奖项、受保护角色或未经确认的卖点；不得把参考图做成直接换产品的近似复制。

## 5. 保真约束与生成方式

- 像素保留约束：指定产品、背景或局部区域必须保持原像素内容，仅允许可验证的抠图、遮罩、合成或局部校色等确定性处理。
- 产品保真生成：允许重新生成光影和环境关系，但用户确认的产品身份、关键结构、颜色与配件不得改变。
- 创意重建：允许生成式重构画面；未验证的角度或结构必须明确标记。用户明确把结果指定为产品母版时，可以按其工作角色使用，但不能把它描述为原始证据已经验证的真实产品事实。

三者不是互斥等级。一次任务可以同时要求“背景遵守像素保留约束”与“产品采用产品保真生成”。Skill 应按素材或区域分别记录适用约束并选择方法；只有素材身份不清楚、无法判断用户要进入路径 B 还是路径 C 时才追问。进入路径 B 后，不再要求用户预先选择 A / B 制作方法。

所有路径统一使用同一个提示词生成顺序：先写输出角色与尺寸，再写产品证据和用户确认事实，再写 Product Structure Map 与目标产品状态；随后锁定共同视觉骨架（叙事、前中后景、焦点、镜头、光线、色彩、材质、家具 / 道具功能和留白），再写 A / B 的高层差异及其联动变化，最后补充物理落位、精确排除项和尺寸处理。提示词必须表达关系，不能只堆形容词，也不能用宽泛的“不要食物 / 不要家具 / 不要生活化道具”误删有用画面元素。每次生成前必须完成提示词覆盖检查，确认已批准的计划没有被静默遗漏。

每次生成前还必须检查镜头与产品落位是否会暴露产品依据未支持的顶部、侧面、背面、内部或接口结构，并确认支撑面、接触点、透视、操作空间和产品主体性同时成立。若存在冲突，先调整镜头 / 落位、改走可保真的候选方向或补充对应角度依据；不能先生成再依靠重复同一方案解决。

## 6. 共同 QA、资产类型与交付状态

所有路径至少检查：任务路径、产品外观、Product Structure Map、颜色 / 配件、文字 / Logo 风险、裁切与遮挡、目标 / 原生 / 最终像素、尺寸处理方式和用户确认状态。

资产类型：

- 产品母版
- 白底交付图
- 场景融合图
- 原创视觉底图
- 创意方向图

交付状态：

- 待确认
- 已确认
- 带已说明风险交付
- 不可作为产品保真交付

资产类型与交付状态必须分开记录，例如：`场景融合图｜已确认`、`创意方向图｜不可作为产品保真交付`。

- 轻微偏差：明确指出具体偏差属于结构、外观、材质、光影、构图或其他维度及其影响；用户接受后可标记为“带已说明风险交付”。不得统一把所有轻微偏差标记为“结构未完全验证”。
- 关键结构风险：必须点名风险并由用户明确接受，才能标记为“带已说明风险交付”。
- 产品身份错误：默认不得称为产品保真交付，标记为“创意方向图｜不可作为产品保真交付”。如果用户仍明确指定它作为产品母版，尊重该指定并保留“用户确认的身份风险”说明，不得省略警示。
- 路径 B 额外检查：Candidate A 是否保留原镜头、空间结构、主要物体与陈列关系；Candidate B 是否保留指定场景身份并形成有意义的创意重拍；两张是否共享产品与业务硬约束、在缩略图下可区分、保持产品第一主体；产品证据是否支持相机计划；比例、透视、接触面、光源、阴影与环境反射是否成立；是否准确区分场景锁定与像素锁定；若降级为单候选，必须记录阻断另一条路线的具体原因。
- 路径 B 额外检查：Candidate A 是否保留原镜头、空间结构、主要物体与陈列关系；Candidate B 是否保留指定场景身份并形成有意义的创意重拍；两张是否共享产品与业务硬约束、在缩略图下可区分、保持产品第一主体；产品证据是否支持相机计划；比例、透视、接触面、光源、阴影与环境反射是否成立；是否准确区分场景锁定与像素锁定；若降级为单候选，必须记录阻断另一条路线的具体原因。对孔位、控制区、顶部开口、接口等身份关键区域，必须检查整块区域，不得用局部修正代替整区判断。
- 路径 C 额外检查：核心表达目标、A/B 实质差异、参考来源距离、信息区、排除项与方案覆盖。

候选组确认规则：Path B / C 的 Candidate A / B 是用于比较的并行候选，不是强制二选一。两张候选都独立通过 QA 且用户回复“无修改意见”时，分别按各自风险记录为“已确认”或“带已说明风险交付”，并作为本轮候选组保留；不要求选出唯一一张。只有后续具体用途需要单张图片时，才在该下游用途发生时选择。若一张候选未通过硬过滤，不能因“无修改意见”保留，必须撤回或按一次定点修订处理。

最终像素以实际生成或导出文件的测量结果为准，不以提示词或生成指令中的目标尺寸为准。必须分别记录目标像素、原生生成像素与最终交付像素；如经过放大、缩放、裁切或补边，需记录处理方式。生成工具不能直接产生目标像素时，应使用可验证的尺寸处理方式完成交付；无法完成时不得通过最终像素 QA。

## 7. 重试、修订与授权边界

- 生成调用发生真实错误、取消、超时或无结果时，只允许一次合理重试。
- 图片已生成但未显示时，只恢复展示，不得重复生成。
- 视觉偏差不自动触发重做；先如实报告，由用户选择接受或提出一个定点修订目标。
- 因未获证据支持的几何结构或镜头 / 落位冲突导致的硬失败，重试前必须改变方案、证据或路径；不得只改写提示词后重复同一计划。
- 路径 B / C 默认生成 Candidate A / B；生成后先请用户提出修改意见或回复“无修改意见”。有修改时一次只接受一个候选的一项优先修订；无修改时不要求二选一。
- 面向用户的中文必须用自然表达。结果卡片先用带符号的加粗句说明“生成和检查已经完成”，再用无序号表格说明方案状态，并用加粗短标签说明产品外观、像素、背景、风险和生成方式；不要把 QA、Gate、Asset、Revision 等内部术语当成用户需要理解的语言。Path A 的下一步表格固定使用三列“下一步｜你可以回复｜接下来会做什么”，产品母版选项统一写“产品母版”，不得写成“产品模板”。
- 当前阶段不自动进入下一路径，不上传外部平台，不覆盖用户原图。

## 8. V1 明确不做

批量 SKU、整套详情页、多参考图混合、自动文案 / 主副标题 / 参数、文字层、Logo 排版、Canva、自动 Marketplace 合规推断、凭单一视角补全并宣称真实的多角度产品图。

## 9. MVP 通过标准

- 路径 A：一个真实案例产生可确认的白底产品母版，并通过产品结构与实际像素检查。
- 路径 B：一个真实案例同时完成“原场景锁定融图”与“场景语言创意重拍”，并通过两种制作逻辑边界、商品主体性、产品保真、场景约束、物理融合与实际像素检查；两张均合格且无修改意见时，允许作为候选组交付，不要求单张选择。
- 路径 C：一个真实案例完成视觉蓝图、A/B 计划确认、原创生成与来源距离 / 产品保真检查；两张均合格且无修改意见时，允许作为候选组交付，不要求单张选择。
- 每个案例的问题必须先分类为输入、业务定义、路径判断、生成偶发、Skill 指令或 QA 责任，再决定是否修改 Skill。

本契约冻结后创建最小 `SKILL.md` 与必要 references；三条路径基本跑通后编写正式 PRD，并进入 V1 收口、回归测试与 Fresh Context 验收。
