# Product Visual Studio V1 Fresh Context 回归报告

日期：2026-08-28  
测试类型：独立 Fresh Context 行为回归（只读）  
测试范围：Path C 生成前路由、候选计划确认、生成后候选组确认语义

## 测试约束

- 不读取本对话历史、旧结论或预期答案。
- 只使用当前 Skill 目录中的 `SKILL.md`、相关 references 和最小素材。
- 不调用 imagegen，不生成图片，不修改工作区文件。

## 测试输入

- 产品母版：`cases/path-a-001/confirmed/ninja-product-master-confirmed-1200x1200.png`
- 视觉参考：`cases/path-c-003/source/xpert-air-fryer-lifestyle-reference-original.png`
- 模拟请求：使用产品母版和参考图，生成一组 1000 × 1000 的 Path C 原创视觉底图。

## 结果

### 1. 路径识别

通过。输入被识别为 Path C：已确认产品母版 + 视觉参考图 + 明确目标像素。参考图只提供场景、光线、层次、人物功能和视觉策略，不继承来源产品、Logo、文案、人物身份或原始空间实现。

### 2. 生成前交互

通过。Skill 要求先展示有实质差异的 Candidate A / B 计划，再用一次确认授权同时生成 A / B；不要求用户先选择 A 或 B，也不把“有人 / 无人”暴露成额外模式选择。

### 3. 生成后交互

通过。若两张候选各自通过产品保真、场景物理逻辑、来源距离、排除项和实际像素 QA，用户回复“无修改意见”时保留两张并分别记录资产类型与交付状态；不强制二选一，只有下游用途需要单张时才选择。

### 4. 硬失败处理

通过。若一张候选硬失败，必须撤回该候选并说明原因；“无修改意见”不能绕过硬过滤。需要修改时，只指定一张候选和一个优先修订点。

## 发现的问题

本次回归未发现与当前 V1 契约冲突的旧交互。该测试只验证行为规则，不替代生成图片后的视觉 QA 或用户真实测试。

## 结论

Fresh Context 生成前行为回归通过，满足 V1 Gate 2 的路由、确认和候选组交互标准；随后已完成 Skill 封版校验和 Codex 安装。
