# Product Visual Studio V1 Project PRD

版本：V1 Project File 版  
对应执行契约：`V1_MVP_CONTRACT.md` v0.8
文档用途：GPT Project 的长期知识底座、视觉生产方法论、判断系统、执行流程和 QA 规则  
日期：2026-09-02  
状态：Project File 定点修订版；Fresh Context Self-Review 通过，Independent Fresh Context Test 待本轮修订后重新执行

## 如何使用本文件

本文件不是功能清单，也不是开发日志。它的目标是让一个没有历史对话、没有先验项目记忆的 GPT，仅凭本文件就能完成一次有边界的商品视觉任务。

执行时先读取本文件的产品定义、证据模型、输入契约和路由规则，再读取与当前路径对应的细节。任何图片生成都必须能够从“素材事实 → 视觉分析 → Candidate Plan → Generation Brief → QA → 用户确认 → 交付状态”逐步追溯。不能把过去对话中的结论、上一张参考图、上一轮候选图或模型自己的想象当作本文件没有写明的事实。

本文件优先解释为什么这样设计、怎样判断和怎样执行；`V1_MVP_CONTRACT.md` 负责 Skill 仓库维护时的 V1 硬边界与版本仲裁；`SKILL.md` 负责运行时入口和文件路由。放入 GPT Project 时，本文件应能独立指导核心业务执行，不把 Contract、SKILL、历史聊天或其他 reference 作为运行前置依赖。若维护阶段发现三者存在硬冲突，应先暂停同步并解决冲突，不能让 GPT 自行猜测。

通用规则的唯一主定义位置为：证据与资产在 PART 2，输入与路由在 PART 3，候选与 Brief 在 PART 8，QA 在 PART 10，用户交互在 PART 11，状态在 PART 12。Path A / B / C 章节只说明这些通用规则在当前路径中的具体应用；若出现重复描述，以对应主定义为准。

# PART 1 — Product Definition

## 1.1 Product Visual Studio 是什么

Product Visual Studio 是一个面向单个 SKU 的、纯图片的商品视觉生产与 QA 系统。它先建立可信的产品母版，再根据用户提供的背景或视觉参考完成白底精修、指定背景融图或参考视觉原创重建。

它不是“多生成几张看起来不错的图”的工具，而是一套控制以下关系的生产方法：

- 哪些信息是产品事实；
- 哪些信息只是参考图提供的视觉方法；
- 哪些内容是合理推断；
- 哪些内容是生成阶段的创意设计；
- 哪些结果可以作为产品保真交付；
- 哪些结果只能作为创意方向或带风险交付；
- 何时需要用户确认；
- 何时必须停止而不能继续生成。

## 1.2 V1 要解决的核心问题

商品生图最容易出现四类错误：

1. 把生成图中的结构、卖点或角度反过来当成产品事实。
2. 把参考图的竞品、Logo、人物、空间或具体版式直接换成用户产品。
3. 只描述“提取视觉风格”或“做两个候选”，却没有说明具体分析维度和生成映射。
4. 图片已经生成，但没有经过产品、物理、来源、像素和用户确认的完整闭环。

V1 通过产品母版中心思想、视觉证据分层、路径路由、Candidate Plan、Generation Brief、分级 QA、Revision 和停止条件解决这些问题。

## 1.3 核心设计原则

### 产品母版中心

所有 Path B / C 商品图都必须从已确认产品母版出发。产品母版是后续商品外观和结构的受控来源。任何生成结果都不会自动升级为 Product Master；如果用户明确指定某张生成图可以作为产品本身或产品母版，必须按用户指定的工作角色使用，并保留生成来源、原始证据边界和已说明风险。Path A 基于可信 Product Source 的保真结果，经 Product Fidelity QA 和用户明确确认后，可以按同一规则提升为新的 Product Master；提升不得扩展原始产品证据与用户确认身份之外的事实边界。

### 证据角色分离

产品图、背景图、视觉参考图和生成图承担不同证据角色。不同图片不得相互越权。

### 先分析，再规划，再生成

视觉分析必须先形成可回看的 Candidate Plan；计划中的重要信息必须映射到候选专属 Generation Brief，不能在提示词中静默丢失。

### 每张候选独立负责

Candidate A 通过不能掩盖 Candidate B 失败。每张候选都必须独立通过产品、物理、场景、来源和像素边界。

### 用户确认是有限的决策点

只在缺失信息会改变方法、事实、风险或交付结果时询问。Path B / C 生成后，用户可以回复“无修改意见”，所有合格候选作为候选组保留，不强制二选一。

### 可停止、可解释、可追溯

每一步都有停止条件；每个风险都说明具体维度和影响；每个交付状态都能追溯到 QA 和用户确认。

## 1.4 V1 Scope

V1 只支持以下纯图片能力：

- 单个 SKU 的产品母版确认与白底产品图精修；
- 将已确认产品母版融入一张指定背景的 Path B，默认双候选；
- 从一张视觉参考图提取视觉方法、重建具体实现并生成 Path C，默认双候选；
- 当客观条件无法形成两个有意义且保真的方向时，允许 Path B / C 降级为单候选并说明原因；
- 产品保真、场景物理、来源距离、视觉层级和实际像素 QA；
- 一次一项的局部 Revision；
- 候选组确认、风险记录和明确停止。

## 1.5 V1 Non-goals

V1 不包含：

- 批量 SKU、批量参考图或整套 Listing / 详情页；
- 多参考图混合；
- 自动 Listing 标题、五点、描述、参数、卖点或其他文案；
- 图片内文字层、Logo 排版、徽章、认证、奖项或图标设计；
- Canva 设计、Canva Handoff、外部上传或自动保存设计；
- 自动 Marketplace 合规推断；
- 把生成的隐藏结构、背面、内部、性能或卖点写成真实事实；
- Job Queue、Worker、数据库、持久状态机或 API 调度系统；
- 通过固定模板、换色、换道具或轻微移动制造假 A/B 差异。

# PART 2 — Visual Evidence & Asset Model

## 2.1 证据对象总表

| 对象 | 角色 | 能证明什么 | 不能证明什么 |
|---|---|---|---|
| Product Source / 产品视觉事实基准 | 用户提供的原始产品图或原始商品素材 | 图片中可见的轮廓、颜色、结构、配件、Logo、文字和可见状态 | 性能、认证、未见角度、隐藏结构、未确认 SKU 事实 |
| User-confirmed Identity / 用户确认身份信息 | 用户明确提供或确认的 SKU、版本、颜色、尺寸等 | 图片无法独立证明但用户确认的身份事实 | 自动证明图片中未出现的结构或功能 |
| Product Master / 产品母版 | 经过检查并由用户确认、供后续生产使用的产品图 | 后续任务应保持的商品身份、外观和已确认可见结构 | 新生成角度、隐藏部件、未确认功能 |
| Specified Background / 指定背景 | Path B 的具体场景来源 | 原背景的镜头、空间结构、主要物体、陈列关系和场景身份 | 用户产品事实、卖点或对生成式细节不现实的像素锁定承诺 |
| Visual Reference / 视觉参考图 | Path C 的视觉方法来源 | 构图、叙事、镜头感觉、层级、光线、色彩、材质、深度、人物 / 道具策略 | 用户产品事实、来源品牌授权、卖点、文案或可直接复制的具体实现 |
| Generated Asset / 生成资产 | 某次生成或修订的结果 | 该次生成实际呈现了什么，可供 QA | 不会自动反向升级为产品事实来源；只有用户明确指定其工作角色后，才能按指定角色使用 |
| Candidate / 候选 | 同一任务下的一条并行视觉实现 | 一条可比较的制作路线及其独立风险 | 自动代表最终唯一选择 |
| Revision / 局部修订 | 针对某张候选一个主要问题的受控更新 | 该问题是否被局部修正并重新 QA | 新增第三条路线或静默改动共同边界 |
| Final Delivery Asset / 最终交付资产 | 经 QA 与用户确认、用于当前明确用途的结果 | 在当前用途和当前状态下可交付 | 永久、无条件、跨用途的产品真实性保证 |

## 2.2 核心证据规则

硬规则：

> 不同图片承担不同证据角色，不得相互越权。

具体含义：

- 只有产品视觉事实基准和用户确认身份信息可以定义产品事实。
- 产品母版是后续商品图的受控来源。任何场景生成结果和普通生成图都不能自动成为产品母版的新版本；用户明确指定某张图可作为产品本身或产品母版时，允许按该工作角色使用，但必须保留用户确认、生成来源、原始证据边界和已说明风险。Path A 保真结果在通过 Product Fidelity QA 并经用户明确确认后，可以按此规则成为新的产品母版。
- 指定背景必须在 Path B 中承担实际场景来源，不得被一句“参考风格”替代。
- 视觉参考图只提供可迁移的视觉方法；它的产品、品牌、人物身份、具体房间实现和卖点不能直接继承。
- 生成图默认只能证明“这次生成画面实际出现了什么”，不能自动证明“产品本来就有这个结构”；如果用户明确指定该图作为产品本身或产品母版，允许按该工作角色使用，但仍保留生成来源和未独立验证说明。

## 2.3 产品视觉事实基准

检查产品原图时，至少记录：

- 完整轮廓、视角、比例和裁切；
- 可见组件数量、拓扑、对称 / 非对称关系；
- 可见面板、抽屉、门、把手、旋钮、接口、支脚、配件和 Logo 位置；
- 可见颜色、材质、纹理、控制件和文字；
- 可以遮挡的区域与不可错误遮挡的身份级结构；
- 当前图片没有验证的角度、背面、内部、隐藏结构和状态。

一张正面图不能证明背面、侧面、内部或精确 3/4 结构。若任务需要这些信息，应补充证据、改写构图使其不可见，或降级为创意方向图 / 带风险结果。

## 2.4 Product Structure Map

Product Structure Map 是从产品视觉事实基准和用户确认身份信息中提取出的结构记录。它不是另一张图片，也不是生成图的描述，而是后续任务用于锁定产品身份和判断结构风险的中间模型。

它至少记录：

- 产品外轮廓、整体比例和当前可见面；
- 组件数量、拓扑、对称 / 非对称关系和层级；
- Identity-critical components，例如门 / 抽屉数量、关键支架、接口、镜头、控制件、把手和功能区；
- Logo、控制面板、可验证文字和关键材质 / 颜色；
- 用户要求必须可见的区域；
- 可以被合理遮挡的区域与不可错误遮挡的区域；
- 当前素材没有验证的角度、背面、内部、隐藏结构和状态。

三者关系固定为：

```text
Product Source / 用户确认身份
→ Product Structure Map（从证据提取的结构模型）
→ Product Fidelity Lock（当前任务冻结的必须保持项）
```

Product Source 是证据；Product Structure Map 是结构模型；Product Fidelity Lock 是针对当前任务从结构模型中选出的执行锁定项。生成结果不能反向修改 Structure Map，也不能把未验证结构写入其中。

## 2.5 产品母版的建立与复用

产品母版建立条件：

1. 产品来源图可读，主体完整，边界和关键结构可检查；
2. SKU、版本、颜色等无法由图片确认的身份信息已由用户提供或确认；
3. 允许的清理、裁切、缩放或保真生成变化已被记录；
4. 用户明确确认该图作为后续生产的产品母版。

如果只有已精修图而没有原始图，可以在用户确认 SKU、颜色、版本和外观正确后使用，但必须标记为“用户确认的产品母版，未与原始素材交叉验证”。

同一已确认 SKU、颜色、版本、可见配件和相关外观仍然清晰，且原产品图可读取时，可以复用产品母版；换一张视觉参考或背景时必须重新分析该新来源，不能继承上一次的场景、镜头、候选或未完成 Revision。

Path A 的保真结果可以作为受控母版候选。结果不会自动成为新的 Product Master；只有在完成 Product Fidelity QA 并经用户明确确认后，才能按用户指定的工作角色提升。Path B / C 的场景生成资产以及普通 Generated Asset 也适用同一用户确认规则。任何提升后的图片都必须继承原始 Product Source、Product Structure Map 和用户确认身份的事实边界，生成过程中新增、猜测或未验证的结构不能获得新的事实权限。

## 2.6 资产类型与交付状态分离

资产类型回答“这是什么图片”：

- 产品母版；
- 白底交付图；
- 场景融合图；
- 原创视觉底图；
- 创意方向图。

交付状态回答“当前能否按说明使用”：

- `待确认`：已生成并完成初步 QA，等待用户检查；
- `已确认`：用户明确接受当前用途和当前风险边界；
- `带已说明风险交付`：有具体、有限、已披露的偏差，用户接受后可交付；
- `不可作为产品保真交付`：产品身份错误、硬结构不支持或来源 / 交付边界严重失败。

示例：`原创视觉底图｜带已说明风险交付`，而不是把“原创视觉底图”当作自动合格证明。

“产品保真交付”是一个受当前证据、QA、用户确认和具体用途共同限定的交付判断，不是独立的图片类型，也不是对所有角度、隐藏结构和未来用途的永久保证。`创意方向图`可以帮助讨论构图和视觉方法，但若产品事实或来源距离不满足要求，不能作为产品保真交付。

# PART 3 — Input Contract & Routing System

## 3.1 输入契约

一次任务至少需要：

| 输入 | 必填性 | 处理原则 |
|---|---|---|
| 产品原始图或已确认产品母版 | Path A 必填；Path B / C 必须有已确认母版 | 只用于产品事实和结构保真，不从生成图补事实 |
| SKU / 版本 / 颜色等身份信息 | 图片不能确认时必填 | 用户确认的身份信息不能被生成图推翻 |
| 指定背景 | Path B 必填 | 是具体场景来源，不是泛风格参考 |
| 视觉参考图 | Path C 必填 | 只提供视觉方法和来源距离分析对象 |
| 最终交付像素 | 所有最终交付必填 | 必须是明确宽 × 高，不能只写“高清”或“8K” |
| 核心表达目标 | 用户未说明时可安全提炼 | 用一句视觉结果描述，不写未经确认的产品卖点 |
| 硬排除项 / 构图边界 | 按需 | 会改变方法或风险时必须纳入共同边界 |

## 3.2 输入身份识别

收到素材后，先给每张图标记角色：

1. 看是否是产品主体、是否完整、是否能核验可见结构；若是，标记产品视觉事实基准或产品母版候选。
2. 看是否定义一张具体场景并要求产品进入其中；若是，标记指定背景。
3. 看是否提供镜头、场景、光线、材质、人物、构图或信息组织方法；若是，标记视觉参考图。
4. 已生成候选不得被重新标记为原始产品或原始参考。

## 3.3 路由表

| Path | 进入条件 | 核心目标 | 主要输出 |
|---|---|---|---|
| Path A | 用户要白底产品图、建立 / 修整产品母版，或明确只处理产品本身 | 保持产品事实，清理背景与外观呈现 | 产品母版或白底交付图 |
| Path B | 已确认产品母版 + 用户指定的一张具体背景，要求把产品融入该场景 | 在同一具体背景身份下比较原场景融图与场景语言创意重拍 | 默认两张场景融合图候选；必要时单候选 |
| Path C | 已确认产品母版 + 一张视觉参考，要求借鉴其视觉方法生成原创商品图 | 保留可迁移方法，重建来源具体实现 | 默认两张原创视觉底图候选；必要时单候选 |

内部可以同时看到背景和参考图，但一次请求只进入一条主 Path：

- 用户要求“产品进入这张具体照片”，优先 Path B；
- 用户要求“参考这种画面 / 氛围 / 视觉表达重新做一张”，优先 Path C；
- 用户只要求产品本身白底处理，进入 Path A；
- 用户意图同时包含两种任务时，先指出这是两个独立任务，请用户确定当前先做哪一个，不自动跨 Path。

Path B 与 Path C 的关键区别：Path B 必须保留这一张指定背景的可识别场景身份；Path C 只迁移参考图的视觉方法，具体空间、人物、道具、产品关系和实现都要原创重建。

## 3.4 何时询问，何时推断

必须询问的情况：

- 产品身份、版本、颜色或关键结构不清，可能改变产品事实；
- 无法判断用户要求的是具体背景融图还是参考方法重建；
- 目标像素缺失或不同尺寸会改变构图；
- 用户要求像素级不变，但当前方法只能生成式重绘；
- 用户要求暴露的角度 / 结构没有产品证据；
- 用户的修改会改变共同边界、核心表达目标或交付用途。

可以安全推断的情况：

- 输入角色清楚，且推断不会改变路径、产品事实或风险；
- 参考图中的人物有明确叙事功能，可以内部提炼为人物策略而不要求用户选“有人 / 无人模式”；
- 目标像素和产品母版明确，候选镜头可以按视觉证据动态制定。

安全推断时要简短说明推断并继续，不要制造多余确认节点。

## 3.5 Task Positioning Card

在任务定位卡之前，只做一次素材回执，确认输入角色，不重复索要已经收到的文件：

```text
🟢 已收到素材
产品母版：已确认 / 待确认。
指定背景或视觉参考：已收到 / 缺失。
目标像素：宽 × 高 / 缺失。
```

素材齐全、路径确定后，用一张紧凑任务定位卡建立共同理解：

```text
🟢 任务定位
任务路径 · 最终像素
目标：一句话说明共同核心表达目标。
保真重点：1–3 个最重要的产品结构或外观事实。
场景视觉语言：叙事、层级、镜头、光色、材质和人物 / 商品关系。
借鉴：可迁移的视觉策略。
改写：必须重建的来源产品、人物、空间、道具和关系。
共同边界：所有候选都不能违反的限制。
```

普通用户不需要看到完整内部 Generation Brief、状态机或 Gate 表；但这些信息必须在内部冻结并可追溯。

# PART 4 — Global Generation Principles

## 4.1 Product Fidelity Lock

根据当前产品证据，建立 Product Fidelity Lock。至少锁定：

- SKU 身份、版本和颜色；
- 外轮廓、比例、组件数量和拓扑；
- 关键可见结构、控制件、配件和 Logo 位置；
- 用户要求必须可见的产品面；
- 不可裁切、不可错误遮挡的身份级区域；
- 已知材质、颜色和可验证文字；
- 用户明确禁止改变的状态或边界。

允许重新生成环境光、阴影、反射、合理接地和被证据支持的小面积遮挡修复；不允许静默增加、删除、合并、移动或虚构身份级结构。

## 4.2 Fact / Inference / Creative 三层分离

对每一项重要信息标记层级：

- 已知事实：来自产品图或用户确认，生成时必须保持；
- 合理视觉推断：由透视、光线、空间或使用关系推测，必须说明其不等于产品事实；
- 创意设计：为完成画面而选择的镜头、道具、人物动作、材质关系或场景实现，不能写成产品卖点。

如果没有证据，不要用“看起来合理”替代“已验证”。

## 4.3 Source Distance

尤其在 Path C 中，必须区分“可迁移策略”和“不可直接继承的具体实现”。需要重建的来源身份锚点包括：

- 来源产品的形态、品牌、Logo、界面、文案、包装、奖项、认证和卖点表达；
- 原始房间几何、柜体 / 窗户 / 家具 / 台面的精确关系；
- 产品坐标、相机路径、裁切、卡片几何和醒目的道具组合；
- 人物的脸、发型、服装、姿势、动作、位置、视线和与商品的关系；
- 受保护角色、独特图形和高识别度的元素组合。

只换产品、换脸、换几件装饰或换颜色，不足以构成原创距离。应保留抽象视觉方法，同时重建具体实现。

## 4.4 Physical Plausibility

商品进入环境至少检查：

- 尺度与比例；
- 相机透视、地平线和空间深度；
- 支撑面、重力、接触关系和 operating clearance；
- 接触阴影、光向、色温和环境反射；
- 人手 / 产品接触、遮挡和操作姿态；
- 功能区与使用逻辑；
- 食物和道具的数量、用途、摆放和材质响应。

拒绝漂浮、穿透、悬空、卡在台面中、无法操作、被人物或道具遮住关键结构的结果。

## 4.5 Scene Plausibility

当参考图和核心目标要求生活化住宅场景时，应有少量与备餐、操作、收纳或清洁区域对应的功能性生活痕迹，同时保留开放工作面。空样板房和无功能的装饰堆砌都不通过场景合理性 QA。

这一规则不适用于刻意极简、棚拍、展陈或其他非住宅场景；此时按该场景自身的密度和功能逻辑判断。

## 4.6 Pixel Integrity

每张生成 / 导出结果必须记录：

- Target Pixel：用户要求的最终宽 × 高；
- Native Generation Pixel：模型实际返回的原生像素；
- Final Delivery Pixel：最终文件实测像素；
- Resize、Crop、Padding 或其他尺寸处理方式。

目标像素不等于原生细节生成像素。尺寸不达标时可以继续视觉检查，但不能称为最终交付；拉伸、不可解释的变形或未经复核的裁切不能通过 Pixel QA。

## 4.7 生成计划必须可追溯

视觉分析中已经确认的 Core Expression Goal、Product Fidelity Lock、场景路线、A/B 差异轴、信息区、人物 / 道具策略、来源改写和排除项，必须在候选专属 Generation Brief 中保留。

提示词可以自然压缩，不能静默遗漏、替换或弱化这些决定性信息。

# PART 5 — Path A Operating Method

## 5.1 Path A 的目标

Path A 用于建立或交付产品本身的白底视觉资产。它优先保证产品外观和结构可信，不追求通过创意场景弥补证据不足。

## 5.2 输入检查

检查：

1. 产品主体是否完整、清晰、边界可读；
2. 视角、轮廓、比例、可见结构、颜色、配件、Logo 和文字；
3. 裁切、遮挡、反光、灰尘、白边、背景干扰和可用像素；
4. 用户是否要求改变产品状态，例如关闭抽屉；
5. 目标像素是否明确。

## 5.3 可处理变化

- 确定性清理：抠图、边缘清理、除尘、保守校色、裁切、补边和可验证缩放；
- 产品保真生成：重建光影、接地阴影、反射或有足够证据支持的小区域；
- 用户明确要求且关键结构有足够证据的可见状态变化。

如果请求需要创造未验证的内部、背面、配件、控制件或关键角度，必须补充证据，或说明只能得到方向 / 风险资产，不能直接建立产品母版。

如果计划中的镜头或产品落位会暴露产品依据没有支持的顶部、侧面、背面、内部或接口结构，必须先调整镜头 / 落位、补充对应角度依据，或改走可保真的候选方向；不得先生成，再靠重复同一方案解决。

## 5.4 Path A Generation Brief

必须写清：

- 编辑目标和只允许改变的区域；
- 要保持的 SKU、视角、比例、材料、颜色、可见控制件、配件、Logo 和文字；
- 白底或允许的纯色 / 非场景背景要求；
- 产品占比、接地阴影和材质分离要求；
- 不得添加的道具、声明、角度、功能和文字；
- 目标 / 原生 / 最终像素与尺寸处理。

## 5.5 Path A QA 与停止

交付一张 `产品母版｜待确认` 或 `白底交付图｜待确认`，同时保留原始素材。报告实际改变、明确保留、生成而非直接验证的区域、像素和风险。

产品结构错误、身份错误或未验证结构被当作事实时，不得建立已确认产品母版。用户确认后才能进入 Path B / C；Path A 不自动进入其他路径。

对孔位、控制区、顶部开口、接口、把手等身份关键区域，必须检查整块区域；局部修正不能代替整区判断。若硬失败来自未获证据支持的几何结构或镜头 / 落位冲突，后续重试必须先改变证据、镜头、落位或路径。

# PART 6 — Path B Operating Method

## 6.1 Path B 的目标与输入

Path B 使用一张已确认产品母版和一张用户指定背景，把产品放入这一具体场景。默认规划两张候选；如果客观条件无法形成两个有意义且保真的方向，允许降级为单候选并说明原因：

- Candidate A｜原场景锁定融图；
- Candidate B｜场景语言创意重拍。

候选共享产品母版、SKU、目标像素、核心表达目标和产品保真边界。

## 6.2 指定背景分析

生成前分析并记录：

- 原始镜头、画幅和可见范围；
- 空间结构、支撑面、层次和 circulation；
- 主要物体、陈列关系和遮挡关系；
- 场景身份、材质、主色、光源、光向、色温和反射；
- 视觉重心、产品可落位区域和功能区逻辑；
- 目标画幅与背景画幅不同时的裁切、补边或重构影响。
- 背景镜头会露出的产品面，以及每个关键面是否有产品依据或用户确认；
- 产品落位的支撑面、接触点、比例、透视、操作空间和缩略图主体性。

背景身份不是“暖色厨房”这类泛标签，而是这一张具体背景可识别的空间、材质、镜头和关系组合。

## 6.3 Candidate A：原场景锁定融图

目标：让结果看起来像产品真实放进用户上传的这张背景。

必须尽量保留：

- 原镜头和空间结构；
- 主要可见物体、支撑面和陈列关系；
- 场景身份、光线和整体关系。

允许：

- 为目标比例做已说明的裁切或补边；
- 增加产品、接触阴影、必要反射和局部光色融合；
- 在用户明确许可下进行有限的局部处理。

像移除桌布、移动小型道具等背景局部调整，生成前只能作为建议；除非用户明确提出或确认，不得写成已经获得授权。

不自动承诺背景像素级不变。若用户明确要求像素不变，应改用可验证的确定性抠图、遮罩、合成和局部校色；能力不足时停止或请求用户放宽约束。

Candidate A 不适合生成的情形：没有有效支撑面、产品无法物理落位、背景视角完全不支持产品证据，或确定性像素约束与当前方法冲突。需要降级为单候选时，说明具体阻断原因，不能为了双候选制造弱结果。

如果 Candidate A 的背景镜头与产品落位会暴露未验证的顶部、侧面、背面、内部或接口结构，必须先改用可支持的落位（例如更高的桌面）、调整镜头或补充产品依据；在冲突消除前不得生成 Candidate A。该类硬失败后的重试不得只改写提示词而重复同一方案。

## 6.4 Candidate B：场景语言创意重拍

目标：保留这一具体背景的可识别场景身份和视觉语言，为产品重新设计商品图。

必须保留：

- 场景身份和足够的空间 / 设计锚点；
- 材质、色彩、光线、气质和使用语境；
- 产品第一视觉主体和共同产品边界。

允许重新设计：

- 镜头高度、方向、景别、产品位置和深度关系；
- 周边物体安排、阻碍商品展示的物体和克制的相关食物 / 道具；
- 商品展示感与生活体验感的比例。

不能变成完全不同的房间或泛化新概念，也不能虚构产品母版不支持的结构。Candidate B 仍是 Path B，因为同一具体场景身份必须存续；可迁移方法的自由重建属于 Path C。

## 6.5 Path B A/B 实质差异

A/B 必须在制作逻辑或高层视觉呈现策略上有实质差异，不能只靠：

- 放大 / 缩小；
- 左右移动；
- 小幅换机位；
- 增加一个小道具；
- 轻微光影、颜色或裁切变化。

如果背景条件、产品证据或用户边界不支持两条有意义且保真的路线，允许只生成一个候选，并说明为什么另一条被阻断。

最低可执行判断：每个有效 Candidate B 必须命名一条高层差异轴，并让至少两项联动后果随之改变，例如机位 + 产品占比、产品—人物关系 + 景深、功能区 + 空间深度。只改变一个小道具、颜色、裁切或几像素位置，属于 Fake Difference。

## 6.6 Path B 生成前冻结项

- 指定背景实际文件和背景身份分析；
- 产品母版、SKU、版本、颜色和 Product Fidelity Lock；
- 目标像素、画幅适配和像素约束；
- Core Expression Goal；
- Candidate A 的镜头、落位、场景保持和物理融合；
- Candidate B 的场景身份保持、镜头和创意重拍轴；
- 产品第一主体、道具 / 食物 / 人物边界；
- 来源、Logo、文字、结构和排除项；
- 每张候选的实际风险。

## 6.7 Path B QA

除了统一 QA，还要分别检查：

- A 是否真的保留原镜头、空间结构、主要物体和陈列关系；
- B 是否真的保留指定场景身份并形成创意重拍；
- A/B 是否共享商品硬约束且缩略图下可区分；
- 产品是否为第一主体；
- 产品证据是否支持镜头和可见面；
- 尺度、透视、支撑、阴影、光源、反射和功能区是否成立；
- 是否准确区分“场景锁定”和“像素锁定”。
- 孔位、控制区、顶部开口、接口等身份关键区域是否完成整块检查。

## 6.8 Path B 结果确认

候选生成并独立 QA 后，向用户展示实际实现、实际像素和主要风险，询问是否有修改意见。若用户回复“无修改意见”，所有合格候选都保留并分别记录状态；不要求二选一。若用户提出修改，只接受一张候选的一个优先修订点。只有后续用途确实需要单张图时，才在那个下游节点选择 A/B。

Path B 的生成前一次候选计划确认是当前 Path B 运行时交互要求，确认的是“按 A/B 计划生成”，不是让用户选择制作方法或选择唯一候选。

# PART 7 — Path C Operating Method

## 7.1 Path C 的目标与输入

Path C 使用一张已确认产品母版和一张视觉参考图，提取可迁移的视觉方法，重建新的产品视觉实现。它始终是纯图片、围绕用户产品的原创视觉底图，不输出无产品底图、不加文字层、不做 Canva。

参考图可能是：

- 来源商品替换参考：参考图有明确商品主体，需要由用户商品承担相似的商业 / 叙事角色，但不能保留来源商品身份、形态、品牌、Logo、文案或卖点表达；
- 场景主导参考：参考图没有需要直接替换的商品主体，学习其场景和视觉逻辑，再给用户产品安排清晰、物理可信的角色。

人物是否出现是参考图驱动的内容分支，不是让用户选择的独立模式。参考图有人物时提取人物叙事功能并重建具体实现；参考图无人物时默认不添加人物，除非用户明确要求或确认的候选计划使其成为必要且实质的变化。

## 7.2 Visual Blueprint System

生成前必须建立 Visual Blueprint。每个维度都要回答“它是什么、对生成有什么作用、哪些可以迁移、哪些必须重建”。

| 维度 | 分析内容 | 生成作用 |
|---|---|---|
| Core Expression Goal | 图片最终让观者感受到的视觉结果，例如产品成为温暖备餐场景的主要主体 | 约束 A/B 必须共同服务的目标，不写未经确认的性能或卖点 |
| Narrative | 画面传达的时刻、动作或体验关系 | 决定产品、人物、食物和空间的叙事职责 |
| Product Role | 商品是英雄主体、场景参与者、功能节点还是体验锚点 | 决定商品占比、可见面和视觉主次 |
| Camera / Lens Feeling | 平视 / 高机位、正面 / 对角、亲密 / 环境、浅景深 / 分层深度的感觉 | 形成可执行的机位和景别，不虚构未经验证的技术参数 |
| Composition | 主体位置、留白、视线和画面平衡 | 决定产品落位、层级和目标画幅重排 |
| Spatial Structure | 前景 / 中景 / 后景、开放度、支撑面、工作区和空间关系 | 防止只复制装饰清单，建立可落地的空间逻辑 |
| Visual Hierarchy | 产品、人物、食物、道具、光斑和背景的注意力顺序 | 防止人物 / 食物抢夺产品主体 |
| Lighting | 光源方向、柔硬、对比、时间感、产品与环境的光关系 | 生成接地、反射、阴影和场景气氛 |
| Color System | 主色、中性色、强调色、冷暖平衡 | 维持视觉语言但不复制具体实现 |
| Material Language | 木、石、金属、织物、玻璃等的材质对比和纹理密度 | 建立环境可信度和产品材质分离 |
| Props / Food Function | 道具或食物用于建立使用、尺度、完成度、深度还是气氛 | 保持用途、数量和摆放合理，不制造功能宣称 |
| Human Presence | 人物的叙事职责、与产品关系、演示 / 生活支持程度 | 只迁移功能，重建身份、姿态、动作、服装和位置 |
| Information Zone | 低干扰但融入场景的视觉承载区 | V1 只规划空间，不加入文字、Logo 或图标 |
| Negative Space | 主体周围的开放区域和操作余量 | 保障主体清晰、后续可用性和真实空间感 |
| Atmosphere | 安静、温暖、忙碌、精致、日常等整体情绪 | 约束光色、密度、动作和材质的协调性 |
| Functional Traces | 备餐、操作、收纳、清洁等少量生活使用痕迹 | 住宅场景避免空样板房，同时避免装饰堆砌 |
| Source Identity Anchors | 造成来源过近的产品、空间、人物、关系和独特组合 | 形成改写清单和来源距离 QA |

Visual Blueprint 的“场景视觉语言”要描述关系，例如“人物通过出餐动作把产品和餐食连接成暖日光下的生活瞬间”，不能只写“暖色厨房”或列装饰品。

## 7.3 Transferable Strategy 与 Non-transferable Implementation

可迁移的是关系和方法：叙事、层级、镜头家族、光色关系、材质对比、空间深度、人物功能、道具用途、信息区逻辑和气氛。

必须重建的是具体实现：来源产品、Logo、文字、人物身份、姿态、服装、精确房间几何、家具组合、产品坐标、相机路径、裁切、道具排列、卡片几何和独特关系。

“换产品”“换脸”“换几件装饰”“把 Logo 擦掉”都不等于来源距离合格。

## 7.4 Path C Candidate A / B 设计

Candidate A 先让提取出的视觉方法对当前产品成立：保留有效的场景语法、层级、光色、材质、深度和叙事功能，重建具体空间、人物、产品坐标和道具关系。Path C 默认规划两张候选；如果客观条件无法形成两个有意义且保真的方向，允许降级为单候选并说明原因。

Candidate B 与 A 共享产品事实、核心目标、画幅、共同边界和来源排除项，但沿一条命名的高层差异轴形成第二条路线。可用差异轴包括：

- 镜头高度、方向、景别或深度；
- 产品主导度、位置或朝向；
- 产品—人物—场景关系；
- 产品演示感与生活体验感的比例；
- 使用叙事、功能区或空间组织；
- 光线情绪或信息区组织（只有确实相关时）。

A/B 不能只改小道具、轻微色彩、极小位置、脸、裁切或单一光影。必须根据当前参考图和核心目标动态找到差异轴；不同房间如果没有共享的视觉语言，不是有效 Candidate B，而是无关的新概念。

最低可执行判断与 Path B 一致：Candidate B 必须命名一条高层差异轴，并让至少两项联动后果随之改变，例如镜头 + 产品占比、产品—人物关系 + 景深、功能区 + 空间深度。只改变单一装饰或视觉微调，属于 Fake Difference。

缩略图下，A 和 B 应被看出是两条不同的视觉路线，但都在回答同一个 Core Expression Goal。

## 7.5 人物、食物和道具规则

参考图有人物时：

- 只保留人物的叙事功能，不保留身份；
- 重建脸、发型、服装、姿态、动作、视线、位置和环境关系；
- 手部与产品接触要可信，不遮挡 Logo、控制件、把手或身份级结构；
- 人物动作不能暗示未验证的机制；
- 人物和食物不能取代产品作为共同目标中的第一主体。

参考图无人且计划不添加人物时：

- 不出现人物、手、身体局部、倒影或人形轮廓；
- 用产品落位、场景状态、食物、工具或道具表达使用逻辑；
- 保留场景的叙事、功能区、密度、光线、材质和深度，不能把无人物理解为空背景。

## 7.6 Path C 生成前确认

Path C 必须先向用户展示一次任务定位卡和实质不同的 Candidate A / B 表，再等待：

- 用户确认按计划同时生成 A/B；或
- 用户提出一项执行型调整。

讨论、比较或提问不等于授权生图。用户明确说“按这个生成”“B 改成低机位后生成”等，才是执行授权。确认后不要求用户先选 A 或 B。

## 7.7 Path C 生成后收口

生成后展示实际图片、A/B 一句话差异、目标 / 原生 / 最终像素、主要风险和 QA 结论。询问用户是否有修改意见，或是否可以按当前结果保留。

若用户回复“无修改意见”，所有通过独立 QA 的候选都保留并分别记录状态；不强制唯一选择。若某张硬失败，只撤回该候选并说明原因；另一张没有修改意见时可单独确认。若用户提出修改，只指定一张候选和一个优先修订点。

# PART 8 — Candidate Plan & Generation Brief

## 8.1 Candidate Plan 的作用

Candidate Plan 是生图前的可回看方案基线，同时是 Generation Brief 和 Coverage Pass 的唯一来源。它不是装饰性表格，也不是生成后对计划的追认。

## 8.2 六行 Candidate Plan 表

Path B / C 默认使用以下六行结构，内容必须根据当前素材动态填写：

| 项目 | Candidate A | Candidate B |
|---|---|---|
| 共同目标 | Core Expression Goal | 相同 |
| 商品位置 / 镜头 | 机位、方向、景别、位置、占比、可见面 | B 的机位计划和空间差异 |
| 场景路线 | 如何落实当前场景 / 参考视觉语言 | 如何在同一语言家族中落实差异轴 |
| A/B 区别 | A 的主要表达取向 | 命名的高层差异轴及其价值 |
| 信息区 | 连续材质、位置和构图作用，或“不设信息区” | 相同或说明为何改变 |
| 重建边界 | 必须重建的产品、人物、空间、道具、关系和排除项 | 相同边界及 B 的特有改写 |

表格之外，任务定位卡还必须给出目标像素、1–3 个产品保真重点、可迁移策略和共同边界。

## 8.3 Generation Brief 必须冻结的字段

每一张候选的 Generation Brief 至少包含：

- 当前 SKU、产品母版和输入角色；
- Path；
- Core Expression Goal；
- Target Pixel、画幅适配和禁止裁切 / 遮挡区；
- Product Fidelity Lock 和 Product Structure Map 相关重点；
- 场景 / 参考视觉策略；
- Candidate 专属镜头、位置、占比、可见面、空间和层级；
- A/B 差异轴及联动变化；
- 人物、食物、道具和信息区策略；
- Source Distance 和必须重建的身份锚点；
- Exclusion List；
- 已知风险和允许的生成推断；
- Preserve：必须保留的产品、场景和视觉关系；
- Exclude：与当前风险直接对应的具体避免项；
- Replacement：被排除内容如何替代或重新组织（适用时）；
- 预期输出资产类型和初始状态 `待确认`。

## 8.4 Candidate Plan Coverage Pass

每次 Candidate A、Candidate B 或 Revision 生图前，逐项对照：

| 已确认计划项 | Brief / prompt 必须体现 |
|---|---|
| Core Expression Goal | 本候选共同服务的视觉结果 |
| 商品位置 / 镜头 | 机位、位置、占比、可见面和空间关系 |
| 场景路线 | 场景、叙事、光线、材质和深度 |
| A/B 区别 | 命名差异轴及协调变化 |
| 信息区 | 连续材质和构图作用，或不设信息区 |
| 重建边界 | 必须重建而非复用的来源锚点 |
| 产品保真重点 | Product Structure Map 中相关结构和外观 |

Coverage Pass 允许自然压缩，但禁止静默遗漏、替换或弱化。若重要计划项不能进入 Brief，先停下来修正计划，不直接生图。

## 8.5 生成前轻量预检

每次调用前确认：

- 原始产品来源 / 已确认产品母版仍可读取；
- Path B 的原始背景或 Path C 的原始参考仍可读取；
- 当前镜头和产品落位只会露出有依据或已获用户确认的产品面；
- 产品有真实支撑、接触点、合理比例和足够主体性；
- Target Pixel、计划和 Brief 相互一致；
- 没有把上一轮 AI 候选当成原始事实来源；
- 当前调用绑定清楚的任务、候选或 Revision 标签。

# PART 9 — Generation, Failure, Fallback & Revision Protocol

## 9.1 Generation Success

调用返回有效图片资产即视为生成成功。有效资产必须真实存在、可读取 / 可展示、绑定到当前任务和 Candidate / Revision，且不是空文件、占位图或无法测量的无效返回；像素不达标属于后续 Pixel QA 问题，不要误报成调用失败。不能把 A 的结果记到 B，也不能把上一轮结果误当作本轮结果。

## 9.2 三类失败必须分开

### Generation Failure

真实调用报错、超时、取消或没有返回资产。只允许一次合理重试；重试仍失败时停止并报告原因。

### Display Failure

图片已经生成，但聊天界面没有正确显示、缩略图缺失或转发失败。只恢复展示或转发已有资产，不得重复生图。

### QA Failure

图片生成成功，但产品、物理、场景、来源、像素或业务边界不合格。按问题等级决定撤回、一次局部 Revision 或重新生成，不能把 QA 失败误报成调用失败。

如果失败原因是未获证据支持的几何结构或镜头 / 落位冲突，后续重试必须先改变证据、镜头、落位或路径；不得只改写提示词后重复同一方案。

## 9.3 Fallback

如正常生成环境出现网络错误、超时或请求中止，且一次合理重试仍失败，只有当前运行环境明确提供、且系统或用户已授权的备用生成能力存在时，才允许恢复单张指定候选。Fallback 只执行已确认的 Brief，不重新决定路径、产品事实、候选逻辑或用户边界。结果返回后仍必须回到主任务完成 QA。

不得假设备用环境、工具或权限存在；若不存在可验证的备用能力，重试失败后直接停止并报告。Fallback 不引入 Job Queue、Worker、数据库、持久状态机或自动调度。

## 9.4 Revision 规则

用户指出修改后，先锁定：

```text
🟢 本次修订
只调整一个已确认的主要目标；保留对应候选路线、产品事实、共同边界和最终像素。
```

一次 Revision 只处理一张候选的一个主要问题。不得因为用户指出一个问题就偷偷同时改镜头、产品大小、道具、光线、人物和场景。

Revision 后必须重新执行受影响的 QA、像素检查和风险记录。Revision 不是新的 Candidate B，也不能借修订之名改变 Core Expression Goal、Product Fidelity Lock、Target Pixel 或 Path。

## 9.5 Revision 与重新生成的判断

| 问题类型 | 处理 |
|---|---|
| 局部构图、轻微道具、局部光影、局部密度问题，且核心事实和路线仍成立 | 一次单点 Revision |
| 关键结构、SKU 身份、产品主次、场景物理或来源距离失败 | 撤回或重新生成；不能用小修掩盖 |
| 目标像素、画幅、核心目标或共同边界改变 | 回到计划阶段，重新确认后再生成 |
| 仅图片未显示 | 恢复已有显示，不生成 Revision |

# PART 10 — Unified QA System

## 10.1 QA 总原则

生成成功不等于业务成功。每张 Candidate / Revision 必须独立 QA；先检查硬边界，再比较审美价值。漂亮但事实错误的图不能交付；较朴素但事实和物理成立的图可以保留为候选。

## 10.2 Product Fidelity QA

检查并报告：

- SKU、版本、颜色和身份；
- 轮廓、比例、组件数量、拓扑、对称关系；
- 可见面板、抽屉、把手、控制件、配件、Logo 和文字；
- 产品状态、视角和要求可见的结构；
- 关键结构是否被遮挡、合并、增加、删除或虚构；
- 当前镜头露出的每个产品面是否都有产品依据或用户确认；
- 孔位、控制区、顶部开口、接口、把手等身份关键区域是否完成整块检查；局部修正不能代替整区判断；
- 材质、颜色、纹理和控制面板文字是否有漂移。

## 10.3 Physical Integration QA

检查：

- 尺度、透视、地平线、支撑面和重力；
- 产品与台面 / 地面的接触、接触阴影和 operating clearance；
- 光向、色温、反射和环境光；
- 边缘、抠图光晕、穿透、漂浮和不可能交叠；
- 人手、人物动作、道具和食物与产品的接触关系。

## 10.4 Scene QA

检查：

- 核心叙事是否成立；
- 产品是否保持第一视觉主体；
- 前景 / 中景 / 后景和视觉层级是否合理；
- 场景身份、材质、色彩和光线是否真实出现；
- 道具 / 食物是否有用途、数量合理且不造成 unsupported claim；
- 住宅场景是否有少量功能性生活痕迹且保留开放工作面；
- 人物是否辅助而非替代产品。

产品第一主体的最低判断是：在缩略图或快速浏览时，观者先识别到用户产品的角色和轮廓，而不是先被脸、食物、道具、光斑或前景物吸走注意力。无需设定统一百分比，但若产品明显退为背景或辅助物，不能通过产品主体 QA。

## 10.5 Path B Logic QA

- A 是否是原场景锁定融图而不是新拍；
- B 是否是同一具体场景身份下的创意重拍而不是完全换场景；
- A/B 是否有制作逻辑层面的实质差异；
- 是否准确说明了生成式背景重绘和像素锁定的区别。

## 10.6 Path C Source Distance QA

检查用户产品是否仍像来源产品、来源 Logo / 文字是否残留、人物身份 / 姿态 / 服装 / 关系是否过近、房间几何和道具组合是否直接复现、镜头 / 裁切 / 卡片结构是否形成编辑参考感。

视觉方法可以相似，具体实现不能形成“直接换产品”的近似复制。

## 10.7 Pixel QA

从实际文件测量并报告 Target、Native、Final Pixel；核对 Crop、Resize、Padding 和是否有拉伸、变形或不可解释的裁切。Final Pixel 不达标时不能称为最终交付。

## 10.8 问题等级

### Hard Fail

违反产品身份 / 关键结构、核心产品证据、来源距离、像素基础要求或重大物理逻辑。处理：撤回、重新生成或有针对性修订；不可作为产品保真交付。

### Risk

无法完全确认但风险具体、有限、可披露，且产品身份仍可用。处理：指出维度和影响；用户接受后可标记 `带已说明风险交付`。

### Soft Issue

审美、轻微构图、局部密度或可优化问题，不影响产品真实性和基本使用逻辑。处理：交给用户决定是否做一次单点 Revision。

不要把所有轻微偏差都写成“结构未验证”；应准确区分结构、外观、材质、光影、构图、文字 / Logo、来源和像素问题。

“无修改意见”只足以确认已经披露、有限且不影响产品身份可用性的普通 Risk / Soft Issue。关键结构风险、身份不确定或其他必须由用户明确命名接受的风险，仍需单独取得明确接受；Hard Fail 永远不能因用户没有修改意见而被保留。

## 10.9 QA 结果卡

生成后给用户看的最小结果卡应包含：

```text
🟢 已生成
A：实际生成路线和一句话差异。
B：实际生成路线和一句话差异。
实际尺寸：Target / Native / Final。
🟠 风险与建议
只说明实际观察到的重大风险和影响。
🟡 请复核
如无修改意见，回复“无修改意见”，所有合格候选都保留；如需修改，请指出一张候选和一个优先修改点。
```

计划未实现时，必须报告实际偏差，不能把计划当成结果描述。

# PART 11 — User Interaction Protocol

## 11.1 什么时候需要用户参与

用户只需要在以下类型的节点参与：

- 确认产品母版或产品身份；
- 补充会改变路径、方法、事实、风险或像素的信息；
- Path B / C 生成前确认候选计划，或提出一项执行型调整；
- 生成后提出修改意见、回复“无修改意见”或接受已说明风险；
- 明确当前用途需要单张 A/B 时做下游选择。

## 11.2 什么时候不应打断

不应要求用户重复已经提供的信息，不应让用户在生图前选择内部模式，不应把人物有无变成额外模式选择，不应在计划已经明确时重复确认同一件事，不应在图片已生成但未显示时重新请求生成。

## 11.3 生成前交互

- Path A：产品母版和目标像素明确后可以进入 Path A brief；结果交付前停下确认。
- Path B：展示素材确认、任务定位、六行 A/B 表和一次确认请求；确认后同时生成。
- Path C：展示 Asset Receipt、Task Positioning Card、六行 A/B 表和一次确认请求；确认后同时生成。

讨论、比较或提问不等于生图授权。明确执行语句才授权当前计划范围内的生成。

## 11.4 生成后交互

默认询问“有没有修改意见”。

- “无修改意见”意味着接受当前候选组中所有合格候选的已披露状态，不要求二选一；
- 对普通 Risk / Soft Issue，“无修改意见”可作为对结果卡已明确披露风险的确认；关键结构风险仍需用户明确接受具体风险；
- 有修改意见时，只接受一张候选的一个主要修订点；
- 某候选 Hard Fail 时，无修改意见也不能保留它；
- 后续需要单张图时，在该用途节点再选择 A/B；
- 确认后记录状态并停止，不自动继续其他 Path、文案、文字层、Canva 或外部上传。

Path A 白底图交付后的下一步必须使用三列表格，不得压缩成两列：

| 下一步 | 你可以回复 | 接下来会做什么 |
|---|---|---|
| 到这里结束 | 确认作为白底交付图 | 保留这张白底图，本轮结束 |
| 作为后续产品母版使用 | 确认作为产品母版，并接受已说明风险 | 将这张图按用户指定的工作角色记录，并保留来源和风险 |
| 继续做指定背景融图 | 提供一张背景图 | 先规划“原场景融入”和“同场景重新组织”两种方案，再等待确认 |
| 继续做参考视觉重建 | 提供一张视觉参考图 | 先提取视觉方法并规划两种原创方案，再等待确认 |
| 修改这张白底图 | 指出一个最优先修改点 | 只处理这一项修改，保留其他已确认内容 |

如果用户只给出目标像素，并说其余按系统判断，视为接受已经说明的保守方案，但生成前仍要复述本次图片将承担的工作角色；这不等于用户逐项确认了所有可见细节。

## 11.5 “继续”的含义

“继续”只能表示继续当前已经明确、已授权的下一步。例如在候选计划已确认后可以进入当前候选的生成，在结果已展示后可以进入用户指定的单点 Revision。

“继续”不自动表示：跨 Path、生成更多候选、加入文字 / Logo、进入 Canva、上传外部平台、扩大 SKU 范围或绕过风险确认。若当前存在多个改变结果的可能动作，应先说明可选动作并请求最小必要决定。

## 11.6 Stop Conditions

必须停止的情况：

- 产品身份或关键结构证据不足，且用户未接受降级；
- 目标像素、Path 或关键硬边界未明确；
- 用户要求像素锁定但当前方法无法验证；
- A/B 没有两个有意义且保真的方向，且无法说明单候选降级；
- 生成失败达到一次合理重试上限；
- QA 发现 Hard Fail，尚未完成撤回或修订决定；
- 用户未确认 Path B / C 候选计划；
- 用户确认当前候选组或 Revision 后，没有明确下游授权。

# PART 12 — Asset & Delivery State Model

## 12.1 状态转换

| 事件 | 状态结果 |
|---|---|
| 生成成功、初步 QA 完成、等待用户检查 | `待确认` |
| 用户回复“无修改意见”，候选独立合格 | `已确认` 或按已披露风险为 `带已说明风险交付` |
| 用户明确接受一个具体有限风险 | `带已说明风险交付` |
| 产品身份错误、关键结构不可用、重大来源或交付失败 | `不可作为产品保真交付` |
| Revision 完成并重新 QA | 回到 `待确认`，等待新的用户确认 |

状态必须逐张记录。候选组可以包含一张或两张 `已确认` 或 `带已说明风险交付` 的候选，不因为没有唯一选择而被视为未完成。

## 12.2 三档产品结构风险

- 轻微视觉偏差：不改变可观察身份或关键结构关系；披露具体漂移，用户接受后可风险交付。
- 关键结构风险：关键可见结构可能错误、缺失、增加、合并、移动或错误遮挡；必须向用户明确命名具体风险并取得单独接受，但接受后仍只能标记为 `带已说明风险交付`，不能升级为 `已确认`。
- SKU 身份级错误：门数、层数、关键支架、接口、控制件或其他身份组件实质画错；不可作为产品保真交付，不能仅凭用户接受转为合格。

## 12.3 “最终交付”定义

Final Delivery Asset 不是自动产生的第三种图片类型，而是当前用途下已完成 QA、用户确认并记录状态的结果。若没有下游单图用途，合格的 A/B 候选组可以作为本轮交付集合；不需要强行选出唯一文件。

# PART 13 — End-to-End User Flow

## 13.1 总流程

```text
上传素材
→ 识别素材角色
→ 建立 / 确认 Product Master
→ 确定 Target Pixel 与核心目标
→ Route Path A / B / C
→ 补充会改变方法或风险的最小信息
→ Visual Analysis / Product Structure Map
→ Candidate Plan
→ 镜头、落位与产品依据预检
→ Generation Brief + Coverage Pass
→ 必要的用户确认
→ Generation
→ 区分调用失败 / 显示失败 / QA 失败
→ 实际图片 QA 与像素测量
→ 展示差异、风险和状态
→ 无修改意见或单点 Revision
→ 分别记录 Asset Type / Delivery State
→ Stop
```

## 13.2 Path A 流程

```text
产品图接收
→ 事实与结构检查
→ 确定性清理 / 保真 brief
→ 白底生成或编辑
→ Product Fidelity + Pixel QA
→ 产品母版 / 白底图待确认
→ 用户确认
→ 记录状态并停止
```

## 13.3 Path B 流程

```text
确认产品母版 + 指定背景
→ 背景身份与落位分析
→ 镜头、落位与产品依据预检
→ Task Positioning Card
→ Candidate A/B 六行计划
→ 用户一次确认
→ 分别生成 A/B
→ 分别 QA
→ 展示实际差异、像素与风险
→ 无修改意见保留所有合格候选，或单点 Revision
→ 记录状态并停止
```

## 13.4 Path C 流程

```text
确认产品母版 + 视觉参考
→ 参考角色判断
→ Visual Blueprint
→ Product Fidelity / Source Distance / Exclusion
→ Task Positioning Card
→ Candidate A/B 六行计划
→ 用户一次确认
→ Candidate Plan Coverage Pass
→ 分别生成 A/B
→ 分别 QA
→ 展示实际差异、像素与风险
→ 无修改意见保留所有合格候选，或单点 Revision
→ 记录状态并停止
```

# PART 14 — Examples & Edge Cases

## 14.1 Path B-003 方法论证据

输入：已确认 NINJA 双篮空气炸锅产品母版、暖色石材厨房指定背景、1000 × 1000 目标像素。

判断：进入 Path B，因为用户要把产品放入一张具体背景。

分析与方案：A 保留原镜头、岛台、花瓶、吊灯和陈列关系；B 保留暖色石材、黄铜、木质、奶油色橱柜和自然光，但重新设计镜头与商品陈列。

QA 证据：两张均记录原生 1254 × 1254、最终 1000 × 1000；A 的背景存在生成式细节重绘，但用户明确接受“场景锁定不等于像素锁定”；B 的场景身份和产品第一主体成立。

验证规则：Path B 的 A/B 应代表两种制作逻辑，不应在单一方法内部通过缩放或轻微镜头变化制造候选。当前候选组确认规则下，合格 A/B 不要求用户提前选唯一一张。

## 14.2 Path C-003 方法论证据

输入：已确认 NINJA 双篮空气炸锅产品母版、带人物的 Xpert 空气炸锅生活方式参考图、1000 × 1000 目标像素。

判断：进入 Path C，因为参考图提供视觉方法，而不是要求保留这张具体背景。来源产品、Logo、网址、标语、型号、原人物和原空间几何必须重建。

第一次 QA 发现：住宅空间过于虚假，B 后场像样板房。根因不是需要更多装饰，而是缺少“少量功能性生活痕迹、保留开放工作面”的条件性场景标准。

规则验证：补充该窄规则后，A / B 均通过场景合理性、商品主体、来源距离和像素初步 QA；控制面板小字仍有生成简化风险，按已披露风险记录。用户无修改意见时，两张均可作为候选组保留，不要求二选一。

## 14.3 Edge Case：产品信息无法确认

处理：指出具体缺失的 SKU、版本、颜色或结构信息；在补充前不把生成图当作事实，不建立产品母版。若用户明确接受创意方向用途，仍要标记不可作为产品保真交付。

## 14.4 Edge Case：用户同时上传背景和视觉参考

处理：根据用户目标判断。要求“进入这张背景”走 Path B；要求“借鉴这种视觉表达重新做”走 Path C；无法判断时只问一个会改变路径的问题，不自动同时跑两条 Path。

## 14.5 Edge Case：A/B 没有有意义的双方案

处理：检查是否只是尺度、裁切、道具或轻微颜色变化。若没有两个真实且保真的差异轴，允许降级为单候选，并说明阻断另一条路线的事实或边界。

## 14.6 Edge Case：产品结构生成错误

处理：若是身份级结构错误，标记 `不可作为产品保真交付`，撤回或重新生成；若是可明确披露的关键结构风险，必须取得用户对具体风险的单独接受，但接受后仍只能标记为 `带已说明风险交付`，不能升级为 `已确认`。

## 14.7 Edge Case：来源距离过近

处理：不能只擦 Logo 或替换产品。检查空间几何、人物实现、镜头、裁切、道具组合和关系；必要时撤回并重新设计来源具体实现。

## 14.8 Edge Case：生成成功但未显示

处理：归类为 Display Failure，恢复已有 asset 的展示 / 转发，不重复生成。

## 14.9 Edge Case：用户说“继续”

处理：根据当前最近的明确授权解释下一步。若候选计划已确认，可继续当前生成；若只是结果展示后说“继续”但没有说明修订或下游用途，不自动生成更多图，先给出最小必要选项。

## 14.10 Edge Case：用户说“无修改意见”但一张 Hard Fail

处理：无修改意见只适用于独立合格候选。撤回 Hard Fail 候选，说明失败原因；其余合格候选可按状态保留，不要求用户再为合格候选二选一。

# PART 15 — Gate, Acceptance & Versioning

## 15.1 Gate 1：方法与契约

目标：三条 Path、证据角色、产品母版、候选组、QA 和停止规则有一致的硬边界。

允许：更新方法论、路径参考和真实案例证据。  
不做：批量功能、文案、Canva、外部上传或自动跨 Path。  
可见交付物：`SKILL.md`、`V1_MVP_CONTRACT.md`、Path A/B/C references、案例报告。  
验收：每张图的事实、策略、生成结果和状态可分离；A/B 不强制二选一。  
验证：真实案例 QA、像素测量、文本一致性检查。

## 15.2 Gate 2：Project File / Skill V1 封版

目标：一个没有历史对话的 GPT 能仅凭本文件理解并执行方法。

验收必须同时满足：

- 能识别产品事实基准、产品母版、背景和视觉参考；
- 能正确路由 Path A / B / C；
- 知道 Visual Blueprint 的分析维度和作用；
- 能设计有实质差异的 Candidate；
- 能冻结 Candidate Plan、Generation Brief 和 Coverage Pass；
- 能区分 Generation Failure、Display Failure、QA Failure；
- 能完成产品、物理、场景、来源和像素 QA；
- 能分级处理 Hard Fail、Risk、Soft Issue；
- 能执行一次一项 Revision；
- 知道何时询问、何时推断、何时停止；
- “无修改意见”时不强制 A/B 二选一；
- 不会扩大到 V1 Scope 之外。

## 15.3 Gate 3：发布后真实使用

Gate 3 是用户在 Codex 中用安装后的 Skill 或 GPT Project 完成一轮真实请求后的验证，不是发布前的理论补充。只记录真实失败、真实歧义和真实边界问题；需要修订时建立 V1.1 定点变更，不借机扩大 V1。

## 15.4 Versioning

- V1：本文件定义的三条 Path、证据分离、候选组确认、QA、Revision 和停止规则。
- V1.1：只处理真实测试暴露的局部问题，保留 V1 的 Scope 和硬边界。
- Future Scope：任何批量、多参考图、文字层、Canva、文案或自动外部动作，必须另行立项，不隐式加入 V1。

# PART 16 — PRD、Contract 与 SKILL 的职责边界

## PRD

回答：为什么这样设计、系统怎样理解图片任务、怎样分析、怎样判断、怎样执行、怎样 QA、怎样交互和怎样停止。它是 GPT Project 的长期知识文件。

## V1_MVP_CONTRACT.md

回答：V1 哪些行为是硬约束、哪些必须做、哪些明确不能做、如何仲裁冲突和如何判断 Gate 通过。

## SKILL.md

回答：Skill 何时触发、运行时如何路由、应读取哪些 references，以及实际执行时的最小入口规则。

三者不机械复制。PRD 负责完整方法论，Contract 负责硬边界，SKILL 负责运行时入口；但 PRD 不能把 GPT 执行所需的关键判断推给历史对话。

# PART 17 — Fresh Context 执行自检

每次把本文件单独放入 GPT Project 后，可用以下自检判断它是否足够自包含：

本文件区分两种验证：`Fresh Context Self-Review` 是基于文档覆盖和决策链的静态自检；`Independent Fresh Context Test` 是把历史上下文切断、只提供本文件和测试输入后观察另一个 GPT 的真实行为。前者通过不等于后者已经通过。

1. 给 GPT 一张产品图、一张指定背景和目标像素，是否能识别 Path B 而不是泛泛说“参考风格”？
2. 给 GPT 一张产品图、一张视觉参考和目标像素，是否能识别 Path C，并说明来源产品不能成为用户产品事实？
3. GPT 是否能建立 Product Structure Map，并明确单一视角无法证明什么？
4. GPT 是否能说明 Visual Blueprint 的叙事、层级、镜头、空间、光线、色彩、材质、人物、道具、信息区、功能痕迹和来源锚点？
5. GPT 是否能让 A/B 共享目标和产品边界，同时沿高层差异轴形成实质差异？
6. GPT 是否能把计划逐项映射到 Generation Brief，而不是只说“提取视觉语言”？
7. GPT 是否能在生成后检查实际图片，而不是把计划当成结果？
8. GPT 是否能区分调用失败、显示失败和 QA 失败，并限制重试？
9. GPT 是否能把问题分为 Hard Fail、Risk 和 Soft Issue，并选择撤回、风险交付或 Revision？
10. GPT 是否能在“无修改意见”时保留所有合格候选，不强制二选一？
11. GPT 是否能解释“继续”只推进当前明确授权，而不是扩大范围？
12. GPT 是否能在产品事实、像素、路径或来源边界不清时停止？

如果任一问题只能靠历史聊天回答，说明本 Project File 仍不完整，应先修订 PRD，再继续扩大真实任务测试。

## V1 Project File 结论

Product Visual Studio V1 的完成标准不是“拥有三条路径的说明”，而是让 GPT 能在 Fresh Context 中用同一套证据角色、分析维度、候选计划、Generation Brief、QA、Revision 和停止规则，稳定地把用户素材转化为可解释、可复核、可交付或可明确撤回的商品视觉结果。

本版本的 Fresh Context Self-Review 结论：仅依赖本文件时，GPT 可以完成素材角色识别、产品事实边界建立、Path 路由、Visual Blueprint、Candidate Plan、Generation Brief、Coverage Pass、生成失败分类、分级 QA、单点 Revision、候选组确认和停止判断；不需要历史聊天才能回答这些关键问题。由于本轮已更新用户交互与授权规则，Independent Fresh Context Test 需要在本轮修订后重新执行；实际图片质量、工具调用可用性和最终用户体验仍需在真实任务中验证。
